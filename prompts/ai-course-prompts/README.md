# Prompts

Copy-paste prompts organized by course section. While most features are implemented using spec files loaded via the `/feature` command, some prompts are used directly. Longer prompts are kept here so you can copy them instead of typing them out.
